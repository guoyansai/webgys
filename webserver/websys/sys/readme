# 📁 ASAI Host 配置文件说明

> **版本**: 2.0.0  
> **适用系统**: ASAI Web CMS 2026  
> **配置文件路径**: `webserver/websys/sys/asaihost.json`

---

## 📑 目录

- [概述](#概述)
- [配置参数详解](#配置参数详解)
  - [1. 网站基本配置](#1-网站基本配置)
  - [2. 系统密码](#2-系统密码)
  - [3. ZIP 配置](#3-zip-配置)
  - [4. 时间配置](#4-时间配置)
  - [5. 路径配置](#5-路径配置)
  - [6. 日志配置](#6-日志配置)
  - [7. WebSocket 安全](#7-websocket-安全配置)
  - [8. 包配置](#8-包配置)
  - [9. IP 配置](#9-ip-配置)
  - [10. 默认索引文件](#10-默认索引文件)
  - [11. 485 串口配置](#11-485-串口配置)
  - [12. TCP 配置](#12-tcp-配置)
  - [13. 代理主机配置](#13-代理主机配置)
  - [14. 主机配置](#14-主机配置)
    - [通用参数](#141-通用参数)
    - [URL 重写配置](#142-url-重写配置)
    - [缓存配置](#143-缓存配置-)
  - [15. MIME 类型配置](#15-mime-类型配置)
  - [16. HTTPS 证书配置](#16-https-证书配置)
  - [17. AES 加密配置](#17-aes-加密配置)
- [🎯 缓存配置最佳实践](#-缓存配置最佳实践)
- [📂 配置文件结构](#-配置文件结构)
- [📅 版本历史](#-版本历史)

---

## 概述

`asaihost.json` 是 ASAI Web CMS 系统的**核心配置文件**，集中管理 HTTP 服务器、WebSocket、缓存策略、日志记录、串口通信等各项系统功能。

---

## 配置参数详解

### 1. 网站基本配置

```json
"website": {
  "title": "WEB",
  "ver": "1.0.0.0"
}
```

| 参数    | 类型   | 默认值 | 说明                               |
|---------|--------|--------|------------------------------------|
| `title` | string | -      | 网站标题，用于页面显示             |
| `ver`   | string | -      | 系统版本号（格式：主版本.次版本.修订.构建） |

---

### 2. 系统密码

```json
"password": "asai"
```

| 参数       | 类型   | 说明                     |
|------------|--------|--------------------------|
| `password` | string | 系统管理密码，用于后台登录验证 |

---

### 3. ZIP 配置

```json
"zip": {
  "zipFileNameEncoding": "gbk"
}
```

| 参数                    | 类型   | 可选值           | 说明                       |
|-------------------------|--------|------------------|----------------------------|
| `zipFileNameEncoding`   | string | `gbk`, `utf-8`   | ZIP 文件解压时的文件名编码 |

---

### 4. 时间配置

```json
"tm": {
  "c": 5000,
  "d": 2000
}
```

| 参数 | 类型   | 默认值 | 说明                         |
|------|--------|--------|------------------------------|
| `c`  | number | 5000   | 连接超时时间（毫秒）         |
| `d`  | number | 2000   | 服务器重启延迟时间（毫秒）   |

---

### 5. 路径配置

```json
"path": {
  "mock": "/websys/db/mock/",
  "ip": "../ip"
}
```

| 参数   | 类型   | 说明                     |
|--------|--------|--------------------------|
| `mock` | string | Mock 数据文件存放路径    |
| `ip`   | string | IP 配置文件路径          |

---

### 6. 日志配置

```json
"logger": {
  "lv": {
    "view": 2,
    "record": 1
  },
  "path": {
    "maxsize": 1048576,
    "client": "./webdata/webdb/log/client/",
    "server": "./webdata/webdb/log/server/"
  }
}
```

| 参数                     | 类型   | 默认值      | 说明                       |
|--------------------------|--------|-------------|----------------------------|
| `lv.view`                | number | 2           | 控制台日志级别             |
| `lv.record`              | number | 1           | 文件日志级别               |
| `path.maxsize`           | number | 1048576     | 单个日志文件最大大小（字节）|
| `path.client`            | string | -           | 客户端日志存放目录         |
| `path.server`            | string | -           | 服务端日志存放目录         |

**日志级别说明**：

| 级别 | 值 | 说明                     |
|------|----|--------------------------|
| 关闭 | 0  | 不输出日志               |
| 错误 | 1  | 仅输出错误信息           |
| 警告 | 2  | 错误 + 警告              |
| 信息 | 3  | 错误 + 警告 + 信息       |
| 调试 | 4  | 输出所有日志             |

---

### 7. WebSocket 安全配置

```json
"wssec": ["asai", "", "-"]
```

| 索引   | 说明                         |
|--------|------------------------------|
| `[0]`  | WebSocket 安全密钥前缀       |
| `[1]`  | 安全密钥中间部分             |
| `[2]`  | 安全密钥分隔符               |

---

### 8. 包配置

```json
"pkg": {
  "type": 1
}
```

| 参数   | 类型   | 可选值    | 说明                                   |
|--------|--------|-----------|----------------------------------------|
| `type` | number | 0, 1      | 0=开发模式，1=生产模式（打包部署）     |

---

### 9. IP 配置

```json
"ip": "asai"
```

| 参数 | 类型   | 说明                                                       |
|------|--------|------------------------------------------------------------|
| `ip` | string | 服务器绑定 IP，`asai` 表示自动获取本机 IP                  |

---

### 10. 默认索引文件

```json
"index": "index.html"
```

| 参数    | 类型   | 说明                             |
|---------|--------|----------------------------------|
| `index` | string | 访问目录时默认返回的文件名       |

---

### 11. 485 串口配置

```json
"rt485": {
  "id": "485_1",
  "path": "COM3",
  "baudRate": 9600,
  "dataBits": 8,
  "stopBits": 1,
  "parity": "none",
  "label": "485"
}
```

| 参数       | 类型   | 可选值                          | 说明                 |
|------------|--------|---------------------------------|----------------------|
| `id`       | string | -                               | 串口标识             |
| `path`     | string | COMx / /dev/ttySx               | 串口设备路径         |
| `baudRate` | number | 9600, 19200, 115200...          | 波特率               |
| `dataBits` | number | 5, 6, 7, 8                      | 数据位               |
| `stopBits` | number | 1, 1.5, 2                       | 停止位               |
| `parity`   | string | `none`, `even`, `odd`           | 校验位               |
| `label`    | string | -                               | 串口显示名称         |

---

### 12. TCP 配置

```json
"tcp": {
  "default": {
    "id": "testtcp",
    "host": "127.0.0.1",
    "port": 4001,
    "label": "设备A"
  }
}
```

| 参数    | 类型   | 说明                     |
|---------|--------|--------------------------|
| `id`    | string | TCP 连接标识             |
| `host`  | string | 目标主机 IP 或域名       |
| `port`  | number | 目标端口                 |
| `label` | string | 连接显示名称             |

---

### 13. 代理主机配置 (`proxyhosts`)

```json
"proxyhosts": {
  "default": {
    "maxSockets": 2000,
    "maxFreeSockets": 200,
    "redirect": false,
    "proxyhttptype": "http://",
    "proxyport": 9298,
    "httptype": "http://",
    "port": 9198
  }
}
```

| 参数              | 类型     | 默认值    | 说明                   |
|-------------------|----------|-----------|------------------------|
| `maxSockets`      | number   | 2000      | 最大并发连接数         |
| `maxFreeSockets`  | number   | 200       | 最大空闲连接数         |
| `redirect`        | boolean  | false     | 是否允许重定向         |
| `proxyhttptype`   | string   | http://   | 代理协议类型           |
| `proxyport`       | number   | -         | 代理服务器端口         |
| `httptype`        | string   | http://   | 目标服务器协议类型     |
| `port`            | number   | -         | 目标服务器端口         |

---

### 14. 主机配置 (`hosts`)

#### 14.1 通用参数

| 参数         | 类型      | 默认值 | 说明                           |
|--------------|-----------|--------|--------------------------------|
| `ws`         | number    | 0      | 是否启用 WebSocket             |
| `port`       | number    | 9198   | 监听端口                       |
| `ckhttp`     | number    | 0      | 是否启用 HTTP 连接检查         |
| `ckws`       | number    | 0      | 是否启用 WebSocket 连接检查    |
| `close`      | number    | 0      | 是否关闭此主机                 |
| `maxhttp`    | number    | 2000   | 最大 HTTP 并发连接数           |
| `maxws`      | number    | 2000   | 最大 WebSocket 并发连接数      |
| `maxonline`  | number    | 100    | 最大在线用户数                 |
| `oncors`     | number    | 0      | 是否启用跨域                   |
| `wstype`     | string    | ws://  | WebSocket 协议类型             |
| `httptype`   | string    | http://| HTTP 协议类型                  |
| `cors`       | array     | []     | 允许的跨域来源列表             |
| `mimetypes`  | object    | {}     | 自定义 MIME 类型映射           |
| `hostdirs`   | array     | []     | 静态文件目录列表               |

#### 14.2 URL 重写配置

```json
"weburls": {
  "rewrite": true,
  "webdir": "webclient",
  "keys": ["/cocontrol", "/couser"]
}
```

| 参数      | 类型    | 说明                         |
|-----------|---------|------------------------------|
| `rewrite` | boolean | 是否启用 URL 重写            |
| `webdir`  | string  | 重写后指向的目录             |
| `keys`    | array   | 需要重写的 URL 前缀列表      |

#### 14.3 缓存配置 ⭐

```json
"cache": {
  "default": 3600,
  "maxSize": 5242880,
  "maxTotalSize": 52428800,
  "extensions": {
    ".js": 86400,
    ".css": 86400,
    ".png": -1,
    ".jpg": -1,
    ".html": 0,
    ".json": 600
  },
  "dirs": {
    "/static/": 86400,
    "/assets/": 604800
  }
}
```

| 参数             | 类型   | 默认值     | 说明                             |
|------------------|--------|------------|----------------------------------|
| `default`        | number | 3600       | 默认缓存时间（秒）               |
| `maxSize`        | number | 5242880    | 单个文件最大缓存大小（字节）     |
| `maxTotalSize`   | number | 52428800   | 总缓存容量上限（字节）           |
| `extensions`     | object | {}         | 按扩展名设置缓存时间             |
| `dirs`           | object | {}         | 按文件夹路径设置缓存时间         |

**缓存时间取值规则**：

| 值       | 含义               | HTTP Header 响应头                              |
|----------|--------------------|-------------------------------------------------|
| `> 0`    | 正常缓存（秒）     | `public, max-age=N`                             |
| `= 0`    | 不缓存             | `no-cache, no-store`                            |
| `< 0`    | 永久缓存           | `public, max-age=31536000, immutable`           |

**缓存优先级**：文件夹规则 > 扩展名规则 > 默认值

---

### 15. MIME 类型配置

```json
"mimetypes": {
  ".html": "text/html",
  ".css": "text/css",
  ".js": "text/javascript",
  ".json": "application/json",
  ".png": "image/png",
  ".jpg": "image/jpeg",
  ".gif": "image/gif",
  ".svg": "image/svg+xml",
  ".txt": "text/plain",
  ".zip": "application/zip",
  ".tar": "application/tar"
}
```

---

### 16. HTTPS 证书配置

```json
"httpscert": {
  "key": "-----BEGIN PRIVATE KEY-----...",
  "cert": "-----BEGIN CERTIFICATE-----..."
}
```

| 参数   | 类型   | 说明                     |
|--------|--------|--------------------------|
| `key`  | string | SSL 私钥（PEM 格式）     |
| `cert` | string | SSL 证书（PEM 格式）     |

---

### 17. AES 加密配置

```json
"aes": {
  "lv": 1,
  "keybase64": "AsaiFfQ1hMYYvT2N4Y1BhenJ0RjY0M2cxWTMZXdXMVY=",
  "aad": "asai aes aad",
  "keylength": 256,
  "ivlength": 12,
  "algorithm": "aes-256-gcm"
}
```

| 参数         | 类型   | 默认值         | 说明                         |
|--------------|--------|----------------|------------------------------|
| `lv`         | number | 1              | 加密级别                     |
| `keybase64`  | string | -              | Base64 编码的加密密钥        |
| `aad`        | string | -              | 附加认证数据                 |
| `keylength`  | number | 256            | 密钥长度                     |
| `ivlength`   | number | 12             | 初始化向量长度（字节）       |
| `algorithm`  | string | aes-256-gcm    | 加密算法                     |

---

## 🎯 缓存配置最佳实践

### 推荐配置示例

```json
"cache": {
  "default": 3600,
  "maxSize": 5242880,
  "maxTotalSize": 52428800,
  "extensions": {
    ".js": 86400,
    ".css": 86400,
    ".png": -1,
    ".jpg": -1,
    ".svg": -1,
    ".woff2": -1,
    ".html": 0,
    ".json": 600
  },
  "dirs": {
    "/static/": 86400,
    "/assets/": 604800
  }
}
```

### 配置原则

| 文件类型       | 建议缓存时间          | 说明                               |
|----------------|-----------------------|------------------------------------|
| 图片 / 字体    | `-1`（永久）          | 配合文件名哈希实现版本控制         |
| CSS / JS       | `86400`（1天）        | 配合构建工具生成哈希文件名         |
| HTML           | `0`（不缓存）         | 确保页面实时更新                   |
| JSON 数据      | `600`（10分钟）       | 根据更新频率调整                   |

---

## 📂 配置文件结构

```
asaihost.json
├── website          # 网站信息
├── password         # 系统密码
├── zip              # ZIP 配置
├── tm               # 时间配置
├── path             # 路径配置
├── logger           # 日志配置
│   ├── lv           # 日志级别
│   └── path         # 日志路径
├── wssec            # WebSocket 安全
├── pkg              # 包配置
├── ip               # IP 配置
├── index            # 默认索引
├── rt485            # 485 串口
├── tcp              # TCP 配置
├── proxyhosts       # 代理主机
├── hosts            # 主机配置 ⭐
│   ├── asai
│   ├── default      # 含缓存配置
│   ├── estunweb
│   └── ...
├── mimetypes        # MIME 类型
├── httpscert        # HTTPS 证书
├── asaisn           # 序列号
└── aes              # AES 加密
```

---

## 📅 版本历史

| 版本   | 日期       | 更新内容                                                       |
|--------|------------|----------------------------------------------------------------|
| 1.0.0  | 2024-01-01 | 初始版本                                                       |
| 2.0.0  | 2026-05-26 | **新增**：LRU 缓存、文件监控、智能缓存策略                     |

---

> 💡 **提示**：配置文件修改后**需重启服务器**生效  
> 🔒 **安全建议**：敏感配置（密码、密钥）不应提交到版本控制系统

---

*文档生成时间: 2026-05-26* ✨